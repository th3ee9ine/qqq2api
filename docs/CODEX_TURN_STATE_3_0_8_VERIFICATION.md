# Codex Turn State 3.0.8 验证记录

日期：2026-09-18。默认模型 `gpt-5.5`。真实凭据和完整 Token 不写入此报告。

## 本次行为

- 设置仅提供自动开关和模型范围，删除手动 Token 配置；旧手动字段被忽略。
- 账号出口拿不到新鲜 292 时，最多选择三个启用、未过期且去重的代理出口，执行最多三轮；每次网络尝试十二秒，一组网络探测九十秒上限。
- 312 立即撤销旧值，轮换前持久化撤销信息。恢复只接受新的合格 292；权限、身份和额度拒绝停止轮换。
- 管理员使用日志增加实际发送的 `upstream_turn_state`，支持查看、复制和 CSV 导出；普通用户 DTO 不含此字段。
- HTTP 保存对应尝试的请求头快照；WS 保存建立该连接时的握手请求头。NULL 表示未记录，空字符串表示未携带。

## 自动化检查

- 前端五个相关测试文件：95 项通过；完整生产构建通过。
- service、handler/admin、handler/dto、repository、migrations 五个包的常规测试通过。
- 新增生命周期、池轮换、312 撤销和发送值快照使用 race 检测；已有广泛匹配测试中的两项并行测试会竞争 `gin.SetMode`，不是新增后台状态锁的数据竞争，不能声称整个仓库 race 检查通过。
- repository 的 unit 标签测试验证 SQL 参数和列顺序。
- 独立 PostgreSQL 容器运行全部迁移后，单条、批量、best-effort 和降级写入四条真实 SQL 路径均验证完整值、空值和 NULL 的保存及读回。
- 代理池测试包含：当前出口失败、过期/禁用出口过滤、重复出口排除、轮次及数量上限、第二轮恢复、旧 292 拒绝、认证/权限/配额停止、312 与错误状态同时出现时撤销、五分钟冷却、WS 复用快照、普通用户 DTO 隔离。

## 真实账号联调

使用用户提供的单账号 OAuth 文件，通过本机代理进行六次真实 Responses 请求，总耗时约 29 秒。

| 请求 | 行为 | HTTP | SSE | 状态结果 |
| --- | --- | --- | --- | --- |
| 1 | 初次自动探测 | 200 | response.completed | 获得 292 |
| 2 | 携带已采集值 | 200 | response.completed | 出站日志快照匹配 |
| 3 | 再次携带 | 200 | response.completed | 出站日志快照匹配 |
| 4 | 本地模拟续期时钟后真实探测 | 200 | response.completed | 新 292，与旧值不同 |
| 5 | 本地模拟 312 后真实恢复探测 | 200 | response.completed | 新 292，与撤销值不同 |
| 6 | 携带恢复值 | 200 | response.completed | 出站日志快照匹配 |

本轮六次 SSE 均完成，输出为 OK。自然 312 数量为零；312 的触发由本地模拟，恢复的新 292 来自真实上游，不能据此声称验证了自然 312。账号附件没有真实代理池，因此多出口轮换通过模拟传输验证，未进行真实多 IP 成功率或跨 IP 绑定测试。

真实联调使用内存账号仓库和标准 net/http 适配器；数据库持久化与 WS 行为由独立集成/模拟测试覆盖，不代表生产账号数据库和生产连接池的完整端到端测试。公开封装长度与时间戳不证明上游模型质量。
